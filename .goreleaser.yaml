version: 2

project_name: nuevoviruz

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/nuevoviruz
    binary: nuevoviruz
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

brews:
  - repository:
      owner: ING-Ricardo-Lopez
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    name: nuevoviruz
    homepage: "https://github.com/ING-Ricardo-Lopez/nuevoviruz-memory"
    description: "Persistent memory for AI coding agents. Agent-agnostic, single binary, zero dependencies."
    license: "MIT"
    commit_msg_template: "chore: update nuevoviruz formula to {{ .Tag }}"
